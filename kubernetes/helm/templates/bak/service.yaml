apiVersion: v1
kind: Service
metadata:
  name: {{ include "tensegrity.fullname" . }}
  labels:
    {{- include "tensegrity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tensegrity.selectorLabels" . | nindent 4 }}
